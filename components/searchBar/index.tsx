"use client";

const SearchBar = () => {
  const handleSearch = () => {};
  return (
    <form onSubmit={handleSearch}>
      <div className="searchbar__item"></div>
    </form>
  );
};

export default SearchBar;
